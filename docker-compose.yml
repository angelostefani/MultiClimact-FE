services:
  web:
    build: .
    ports:
      - "8080:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      DatabaseProvider: Sqlite
      ConnectionStrings__SqliteConnection: Data Source=/data/multiclimact.db
    volumes:
      - sqlite-data:/data
    restart: unless-stopped

volumes:
  sqlite-data:
